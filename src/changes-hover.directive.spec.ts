import { ChangesHoverDirective } from './changes-hover.directive';

describe('ChangesHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new ChangesHoverDirective();
    expect(directive).toBeTruthy();
  });
});
